import type { Metadata } from "next";
export const metadata: Metadata = { title: "Start a Project", description: "Tell OceanBrown about your business, challenges, project needs, timeline, and preferred way to communicate.", alternates: { canonical: "/form" } };
export default function FormLayout({children}:{children:React.ReactNode}){return children}
